name: tectup-tour

include:
  - ./compose/spark.compose.yml
  - ./compose/airflow.compose.yml
  - ./compose/minio.compose.yml

volumes:
  minio-vol:
    name: minio-vol
    driver: local
  airflow-db-vol:
    name: airflow-db-vol
    driver: local

networks:
  airflow-net: