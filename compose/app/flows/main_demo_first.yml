id: first
namespace: demo

tasks:
  - id: hello
    type: io.kestra.plugin.scripts.shell.Commands
    taskRunner:
      type: io.kestra.plugin.core.runner.Process
    commands: 
      - set -x
      - echo $(hostname)
      - echo $(pwd)
      - ls -lh /usr
      - python /usr/app/scripts/script01.py